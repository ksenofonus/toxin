mixin enter
  .form.enter-form&attributes(attributes)
    h2.form__title  Войти
    .form-container
      .userdate
        +field('email', 'useremail', 'Email')(class="text-field")
        +field('password', 'userpassword', 'Пароль')(class="text-field")
      +button('войти')(class="button__long")
      .form-bottom
        p Нет аккаунта на Toxin?
        +button('создать', './registration.html')(class="button__border")